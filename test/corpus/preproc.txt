================================================================================
preproc
================================================================================
#include path/to/somefile.c
#include "path/to/somefile.c"

#define foo
#define "foo"
#define ""
#define "foo#bar"

#ifdef foo
#endif

#ifdef "foo"
#endif

#ifdef ""
#endif

#ifdef "foo#bar"
#endif

#ifndef foo
#endif

#ifndef "foo"
#endif

#ifndef ""
#endif

#ifndef "foo#bar"

#else

#endif
--------------------------------------------------------------------------------
(compilation_unit
	(include (preproc_const))
	(include (preproc_const))
	(define (preproc_const))
	(define (preproc_const))
	(define (preproc_const))
	(define (preproc_const))
	(ifdef (preproc_const))
	(ifdef (preproc_const))
	(ifdef (preproc_const))
	(ifdef (preproc_const))
	(ifndef (preproc_const))
	(ifndef (preproc_const))
	(ifndef (preproc_const))
	(ifndef (preproc_const))
	(else)
	(endif))

================================================================================
preproc with invalid syntax
================================================================================
#ifdef foo
THIS DEFINE BREAKS GAME SCRIPT MODULE COMPILATION DO NOT REMOVE IT,
    IT IS NEEDED FOR VARIOUS TESTS
#else
#endif

enum Bar{}
--------------------------------------------------------------------------------
(compilation_unit)
